<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record model="ir.ui.view" id="view_hr_payslip_inherit_form">
        <field name="name">hr.payslip.inherit.form</field>
        <field name="model">hr.payslip</field>
        <field name="inherit_id" ref="hr_payroll.view_hr_payslip_form"/>
        <field name="arch" type="xml">
		
			<xpath expr="//div[@name='button_box']" position="inside">
                    <button name="show_liquidation" type="object" class="oe_stat_button" icon="fa-list-alt"
                        string="Liquidación" invisible="liquidation_count == 0">
                    </button>			
			</xpath>		
		
            <xpath expr="//notebook[last()]" position="inside">
			    <field name="liquidation_count" invisible="1"/>
                <page string="Liquidación" invisible="liquidation_count == 0">
					<field name="liquidation_line_ids" nolabel="1">
                        <list>
                            <field name="liquidation_type"/>
							<field name="date_from"/>
							<field name="date_to"/>
							<field name="days_total"/>
							<field name="days_leave"/>
							<field name="days_neto"/>
							<field name="wage_actual"/>
							<field name="wage_total"/>
							<!-- field name="previou_wage_total"/ -->
							<field name="days_average"/>
							<field name="wage_average"/>
							<field name="subsidio_transporte"/>
							<field name="total_average"/>
							<field name="amount"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

</odoo>
